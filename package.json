{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start:development": "lerna run dev",
    "start:dev:logs": "lerna run dev --parallel"
  }
}
